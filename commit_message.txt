feat: Implement news ranking, update RSS feeds, and prepare for Render deployment

This commit introduces a keyword-based ranking system for news articles, updates the RSS feed URLs, and makes the backend ready for deployment on Render by dynamically setting the listening port from environment variables.